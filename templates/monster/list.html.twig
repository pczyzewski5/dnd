{% extends 'base.html.twig' %}
{% block body %}
    <style>
        .table-item {
            cursor: pointer;
        }
    </style>
    <a class="button is-primary is-fullwidth" href="{{ path('monster_create') }}">Dodaj potworka</a>
    <table class="table is-striped is-hoverable is-fullwidth">
        <thead>
        <tr>
            <th>#</th>
            <th>nazwa</th>
            <th>klasa pancerza</th>
            <th>punkty wytrzymałości</th>
            <th>szybkość</th>
            <th></th>
        </tr>
        </thead>
        <tbody>
        {% for key, monster in monsters %}
            <tr class="table-item" href="{{ path('monster_read', {'id': monster.getIdAsString()}) }}">
                <td>{{ key + 1 }}</td>
                <td>{{ monster.getName() }}</td>
                <td>{{ monster.getArmorClass() }}</td>
                <td>{{ monster.getMaxHp() }}</td>
                <td>10</td>
                <td>
                    <a class="button is-small is-info" href="{{ path('participant_add', {'monsterId': monster.getIdAsString()}) }}">dodaj do potyczki</a>
                    <a class="button is-small is-warning" href="{{ path('monster_update', {'id': monster.getIdAsString()}) }}">Edytuj</a>
                    <a class="button is-small is-danger" href="{{ path('monster_delete', {'id': monster.getIdAsString()}) }}">Usuń</a>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <script src="{{ asset('js/list_helper.js') }}"></script>
{% endblock %}