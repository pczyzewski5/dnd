<div class="small-card-back">
    <div class="title-section">
        <div class="item-title">
            {% if itemCard is defined %}
                {{ itemCard.getTitle() }}
            {% else %}
                Lorem ipsum dolor sit amet
            {% endif %}
        </div>
    </div>
    <div class="description-section description-section-back"
            {{ itemCard is defined and null != itemCard.getImage() ? itemCardImage(itemCard.getImage())|raw }}
    ></div>
</div>